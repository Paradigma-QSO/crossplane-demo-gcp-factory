apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-gcp-factory.fullname" . }}
  labels:
    {{- include "my-gcp-factory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-gcp-factory.selectorLabels" . | nindent 4 }}
